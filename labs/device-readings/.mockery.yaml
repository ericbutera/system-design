with-expecter: true
inpackage: True

filename: "mock_{{.InterfaceName | lower }}.go"
dir: "{{.InterfaceDir}}"
mockname: "Mock{{.InterfaceName}}"
outpkg: "{{.PackageName}}"

packages:
  device-readings/internal/db:
    interfaces:
      DB:
